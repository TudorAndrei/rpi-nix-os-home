# Repository instructions

- Do not use or recommend `nix profile` commands.
- Apply user packages and configuration through the pinned Home Manager flake.
- Use `scripts/apply-home.sh` to activate the configuration.
- Keep Raspberry Pi OS responsible for the kernel, firmware, boot, and hardware services.
